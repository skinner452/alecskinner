# alecskinner.com

# Requirements

- Node 20
- Amplify: `npm install -g @aws-amplify/cli`

# Running

- `npm start`

# Testing

- `npm test`

# Deploying

- Build the app: `npm run build`
- Login to amplify: `amplify configure`
  - Region: `us-east-1`
